server:
  port: 8080

spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: ${AUTH_SERVICE_URL:http://auth-service:8081}
              predicates:
                - Path=/api/auth/**,/api/admin/**

            - id: payment-service
              uri: ${PAYMENT_SERVICE_URL:http://payment-service:8085}
              predicates:
                - Path=/api/v1/payments/**

            - id: booking-service
              uri: ${BOOKING_SERVICE_URL:http://booking-service:8082}
              predicates:
                - Path=/api/v1/bookings/**

            - id: tour-service
              uri: ${TOUR_SERVICE_URL:http://tour-service:8088}
              predicates:
                - Path=/api/v1/ports/**,/api/v1/cruises/**,/api/v1/decks/**,/api/v1/room-types/**,/api/v1/policies/**,/api/v1/packages/**,/api/v1/schedules/**

      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          "[/**]":
            allowedOrigins:
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
            exposedHeaders:
              - Authorization
              - Set-Cookie

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO
